import React from "react";
import AppHeader from "../AppHeader/AppHeader";

const Login = () => {
    return (
        <section id={"login"}>
            <AppHeader/>
            <span>Logowanie</span>
        </section>
    )
};

export default Login;